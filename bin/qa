#!/bin/sh
#
# Run the QA framework against a given URL:
# The QA framework is expected to run aqainst a production environment
# defaults to using 'chrome' and using 'https'
#
# This script instead points to a local development server
#
# @example
#   BROWSER=firefox BASE_URL=https://localhost:3000 ./bin/qa
#
# ------------------------------------------------------------------------------
set -e

BASE_URL=${BASE_URL-'http://localhost:3000'} \
  bundle exec rspec --default-path ui "$@"