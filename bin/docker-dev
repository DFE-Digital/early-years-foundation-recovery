#!/usr/bin/env bash
# ------------------------------------------------------------------------------
set -e

if [[ ! -z "$MSYSTEM" ]]; then
  echo "You are running Windows!"
  PREFIX="winpty"
else
  PREFIX=""
fi

$PREFIX docker-compose \
  -f docker-compose.yml \
  -f docker-compose.dev.yml \
  --project-name recovery \
  up --detach app

$PREFIX docker attach recovery_dev
