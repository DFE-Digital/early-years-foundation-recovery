#!/bin/sh
# ------------------------------------------------------------------------------
set -e

for env in dev test; do
  docker-compose -f docker-compose.yml -f docker-compose.$env.yml build
done
