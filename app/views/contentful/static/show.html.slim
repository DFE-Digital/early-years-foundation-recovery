.govuk-grid-row
  .govuk-grid-column-two-thirds-from-desktop

    h1= @model.heading
    p= @model.subheading
    - if @model.subheading
      .govuk-section-break--visible

    = translate_markdown(@model.body)
