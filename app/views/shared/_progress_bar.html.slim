.govuk-grid-row
  ol.progress-bar.govuk-list role='progressbar' tabindex=0 aria-label=progress_bar.furthest_section
    - progress_bar.nodes.map do |node|
      li.progress-bar--item
        = progress_node(*node[:icon].values)

        div class=node[:line_style]

        p.govuk-body.progress--heading class=node[:heading_style]
          = node[:heading]

- if debug?
  pre.debug_dump
    = progress_bar.debug_summary
