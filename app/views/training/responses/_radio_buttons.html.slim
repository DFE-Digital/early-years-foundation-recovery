= f.govuk_fieldset question.question_name do
  = f.govuk_error_summary

  = translate_markdown(question.body)

  = f.govuk_radio_buttons_fieldset :answers, legend: { text: content.name, hidden: true } do
    = f.hidden_field :answers
    - question.options.each do |option|
      = f.govuk_radio_button :answers,
        option.id,
        label: { text: option.label },
        link_errors: true,
        disabled: option.disabled?,
        checked: option.checked?
