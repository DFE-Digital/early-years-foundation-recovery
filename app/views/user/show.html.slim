.govuk-grid-row
  .govuk-grid-column-full
    h1.govuk-heading-l Manage your account
    h2.govuk-heading-m Your details
    hr.govuk-section-break.govuk-section-break--l.govuk-section-break--visible

    = govuk_summary_list do |your_details|
      - your_details.row do |row|
        - row.key { 'Name' }
        - row.value { @user.name }
        - row.action(text: t('links.change'), href: edit_name_user_path, visually_hidden_text: 'name')
      - your_details.row do |row|
        - row.key { 'Email' }
        - row.value { @user.email }
        - row.action(text: t('links.change'), href: edit_email_user_path, visually_hidden_text: 'email')
      - your_details.row do |row|
        - row.key { 'Password' }
        - row.value { t('password.last_changed', date: @user.password_last_changed) }
        - row.action(text: t('links.change'), href: edit_password_user_path, visually_hidden_text: 'password')

    h2.govuk-heading-m About your other details
    hr.govuk-section-break.govuk-section-break--l.govuk-section-break--visible

    = govuk_summary_list do |other_details|
      - other_details.row do |row|
        - row.key { 'Setting type' }
        - row.value { @user.setting_name }
        - row.action(text: t('links.change'), href: edit_registration_setting_type_path, visually_hidden_text: 'setting_type')
      - other_details.row do |row|
        - row.key { 'Local authority' }
        - row.value { @user.local_authority }
        - row.action(text: t('links.change'), href: edit_registration_local_authority_path, visually_hidden_text: 'local_authority')
      - other_details.row do |row|
        - row.key { 'Role' }
        - row.value { @user.role }
        - row.action(text: t('links.change'), href: edit_registration_role_type_path, visually_hidden_text: 'role_type')

= render 'debug'
